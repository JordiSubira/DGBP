/*package main

import (
	"bytes"
	"fmt"
	"log"
	
	"os/exec"
	"os"
	//"strings"
)

func main() {

	var args []string

	args = os.Args[1:]


	cmd := exec.Command("node")
	cmd.Args = []string{"node","createUser.js"}
	for i:=0 ; i<len(args);i++{
		cmd.Args = append(cmd.Args,args[i])
	}
	fmt.Println(cmd.Args)

	cmd.Dir = "/home/jordi/Fabric1.1/myapp"
	var out bytes.Buffer
	var err2 bytes.Buffer
	cmd.Stdout = &out
	cmd.Stderr = &err2
	err := cmd.Run()
	if err != nil {
		log.Fatal(err)
	}
	fmt.Printf("in all caps: %s\n", out.String())
	fmt.Printf("in all caps: %s\n", err2.String())
}*/